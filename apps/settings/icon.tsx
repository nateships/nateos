import { AppleIcon } from '@/apps/_shared/AppleIcon'
export function SettingsIcon({ size }: { size?: number }) {
  return <AppleIcon src="/apple/icons/system-settings.png" alt="Settings" size={size} />
}
