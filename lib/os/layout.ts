/**
 * Shared layout constants for OS chrome. Single source of truth so the
 * menubar height tracks in lockstep across the menubar component itself,
 * the WindowLayer's spacing, the tile-finder's min-Y, and Window's
 * maximize offset.
 */
export const MENUBAR_H = 28
