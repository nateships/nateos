import type { Profile } from '@/lib/content/schema'
import data from './data.json'

// Regenerated from `content/profile.mdx` via predev/prebuild/pretypecheck.
export const profileData = data as Profile
