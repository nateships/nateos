import type { Bookmark } from '@/lib/content/schema'
import data from './data.json'

// Regenerated from `content/links.mdx` via predev/prebuild/pretypecheck.
export const bookmarks = data as Bookmark[]
